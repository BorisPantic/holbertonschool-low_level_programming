#include "main.h"

/**
 *
 */

void more_numbers(void)
{
int dixfois;
int numbers;

	for (dixfois = '0'; dixfois < '9'; dixfois++)
	{
		for (numbers = '0'; numbers <= 14; numbers++)
			_putchar(numbers);
			_putchar('\n');
	}
}
