#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - print last digit of a number.
 * Return:r
 * @r: number
 */

int print_last_digit(int r)
{
	for (r % 10)
		{
		return (r);
		}
	return (0)
}	
